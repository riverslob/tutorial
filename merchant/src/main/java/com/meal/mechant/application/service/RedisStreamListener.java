package com.meal.mechant.application.service;/*
package com.meal.mechant.application.service;

import com.meal.mechant.domain.MyMessage;
import org.springframework.data.redis.connection.stream.ObjectRecord;
import org.springframework.data.redis.stream.StreamListener;
import org.springframework.stereotype.Component;

@Component
public class RedisStreamListener implements StreamListener<String, ObjectRecord<String, MyMessage>> {
    @Override
    public void onMessage(ObjectRecord<String, MyMessage> message) {
        System.out.println(message.toString());
    }


}


*/
